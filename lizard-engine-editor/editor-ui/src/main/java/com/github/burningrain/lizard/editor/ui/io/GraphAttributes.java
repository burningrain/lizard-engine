package com.github.burningrain.lizard.editor.ui.io;

public enum GraphAttributes {

    // процесс
    PROCESS_NAME,

    // общее у нод и дуг
    ID,
    DESCRIPTION,
    PLUGIN_ID,
    TYPE,
    DATA,

    // нода
    COORD_X,
    COORD_Y,
    CLASSNAME,

    // дуга/ребро
    NODE_SOURCE,
    NODE_TARGET,
    TAG,
    ;

}
